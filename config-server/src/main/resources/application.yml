server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/YadigarAlakbarli/snap
          search-paths:
            - "config-repo/*-configs/"


management:
  endpoints:
    web:
      exposure:
        include: "*"